---
description: Design REST API: resources, endpoints, auth, versioning, error format. Produces OpenAPI spec.
agent: backend-developer
---

Design REST API for: $ARGUMENTS

Follow the api-design skill:

1. Identify resources (nouns, plural, lowercase).
2. Define endpoints: method, path, parameters, request/response schema.
3. Choose auth pattern: JWT, OAuth2, API keys.
4. Define error format and status codes.
5. Add pagination, filtering, sorting patterns.
6. Produce OpenAPI 3.0 spec (YAML).

Report: resources table, endpoint list, auth flow, error format, OpenAPI spec path. Format: structured markdown.
